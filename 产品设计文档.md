# 网页阅读器产品设计文档（v0.1）

## 0. 文档信息

- 文档版本：v0.1
- 更新日期：2026-08-19
- 产品暂定名：网页阅读器（工作名：CleanRead）
- 目标形态：浏览器扩展（Chrome / Edge），后续可扩展桌面端
- 计费服务：AjkAPI
- 计费服务器：`https://ajkapi.9zos.com`
- 默认价格：9.9 元 / 月（包月）
- 默认免费额度：每天 3 篇

> 本文档用于指导设计、开发和发布。AjkAPI 的具体接口名、字段、权限配置需在开发前以其官方文档为准。

---

## 1. 产品定位

### 1.1 一句话定位

一个安装简单、使用简单、界面专业的网页阅读器：把当前网页、多个链接、甚至页面里的嵌套链接，快速转成干净、保真、可导出的 Markdown / 纯文本。

### 1.2 用户为什么不用免费 DeepSeek

免费 DeepSeek 能完成“偶尔清理一篇文章”，但它不是为高频采集和原文保真设计的。

本产品的核心差异是：

| 维度 | 免费 DeepSeek | 本产品 |
|---|---|---|
| 操作步骤 | 打开 AI、复制网址、写提示词、等待生成、再复制 | 一键提取当前页，或直接粘贴多个链接 |
| 原文准确性 | 可能改写、省略、幻觉 | 只提取原文，不改写、不编造 |
| 来源信息 | 不稳定 | 保留标题、作者、日期、来源 URL |
| 批量处理 | 不方便 | 支持多链接队列、嵌套链接 |
| 隐私 | 内容上传第三方 | 本地处理优先 |
| 导出格式 | 需要再整理 | 直接导出 Markdown、纯文本、JSON、HTML |
| 工作流 | 每次手动操作 | 历史、收藏、引用格式、后续接入 Notion / Obsidian / Word |

### 1.3 目标用户

1. 文案工作者：搜集资料、整理素材、引用原文、批量采集。
2. 中小学生：查资料、摘抄、安全阅读、语音朗读。
3. Agent / 开发者：稳定地把网页转成 Markdown，不依赖大模型生成。

### 1.4 使用场景

- 打开一篇文章，一键转成干净阅读模式。
- 把多个资料链接一次性转成 Markdown 笔记。
- 打开一个列表页，把里面多个详情页一起采集。
- 为写作、论文、公众号、短视频文案保留可引用的原文和来源。
- 给 Agent 提供确定性的网页正文输入。

### 1.5 非目标

- 不做独立全网搜索引擎。
- 不做通用 AI 聊天助手。
- 不做复杂爬虫或大规模自动化采集。
- 不承诺抓取登录后内容、验证码页面、需要授权的付费内容。

---

## 2. 总体结论与产品形态

### 2.1 第一版建议做成浏览器扩展

优先做 Chrome / Edge 浏览器扩展，理由：

1. 安装简单：下载后加载即可，不需要安装整套桌面软件。
2. 能直接处理当前页面链接。
3. 能读取页面里的链接，适合做嵌套采集。
4. 浏览器扩展可以本地抓取和清洗，边际成本低。
5. 适合在 GitHub / Gitee 公开发布，方便用户下载。

### 2.2 与“无服务器”的关系

核心抓取、清洗、阅读、导出都在本地完成，不需要自建业务服务器。  
只有登录、余额、月卡、免费额度校验调用 AjkAPI。

因此本产品的“无服务器”指：**不维护自己的网页抓取服务器**，而不是完全不调用任何后端。

---

## 3. 核心功能需求

### 3.1 安装与使用简单

安装要求：

- 提供 Chrome 版和 Edge 版。
- 提供 GitHub / Gitee Release 下载。
- 提供源码压缩包。
- 提供 SHA256 校验文件。
- 提供图文安装说明。
- 提供首次启动引导。

用户安装后应能在 3 步内完成第一次使用：

1. 打开扩展。
2. 登录或注册 AjkAPI 账号。
3. 点击“提取当前页”。

### 3.2 直接处理当前页面链接

- 点击扩展图标，一键提取当前网页正文。
- 支持快捷键触发，例如 `Alt + C`。
- 支持右键菜单“用网页阅读器提取本页”。
- 提取后自动进入阅读模式。

### 3.3 多链接批量处理

- 支持一次输入多个链接。
- 每行一个链接，自动去空行、去重。
- 支持从剪贴板粘贴。
- 支持从当前页面自动读取链接。
- 批量任务显示进度、成功数、失败数、失败原因。
- 批量结果可以合并导出，也可以分别导出。

### 3.4 嵌套网页链接采集

当页面里还有网页链接时，用户可以选择“采集本页内的链接”。

功能要求：

- 自动识别当前页面中的站内链接。
- 可选只采集同域名链接。
- 可选只采集文章详情链接。
- 默认递归深度为 1 层，可配置为 2 层。
- 默认单次最多 20 个链接，可配置。
- 采集前先展示链接列表，让用户勾选。
- 避免重复采集相同 URL。
- 支持中断任务。

### 3.5 网页正文清洗

清洗目标：

- 去除广告、导航、弹窗、推荐、评论区、脚本、样式等干扰内容。
- 保留标题、作者、发布时间、正文、关键图片说明。
- 保留原文措辞，不做 AI 改写。
- 支持中文网页优先，同时兼容英文网页。
- 输出 Markdown 和纯文本两种格式。

清洗质量要求：

- 常见新闻、公众号文章、博客、百科、学习资料站的成功率目标：90% 以上。
- 提取失败时给出明确提示，并允许用户复制原始链接。

### 3.6 专业阅读模式

- 干净的排版：标题、字号、行距、段距、最大阅读宽度。
- 浅色 / 深色 / 护眼模式。
- 字体大小调节。
- 语音朗读。
- 显示字数、预计阅读时间。
- 关键词高亮。
- 支持复制、收藏、导出。

### 3.7 导出与引用

第一版支持：

- Markdown `.md`
- 纯文本 `.txt`
- JSON `.json`
- HTML `.html`
- 一键复制 Markdown
- 一键复制纯文本

后续可扩展：

- Word `.docx`
- PDF
- 导出到 Notion / Obsidian / 语雀 / 飞书

引用格式：

- 复制时附带来源网址。
- 支持普通引用格式。
- 后续可加入 GB/T 7714、MLA、APA 等格式。

### 3.8 历史记录与收藏

- 本地保存处理记录。
- 按时间、标题、来源搜索。
- 收藏重要文章。
- 支持标签。
- 支持清空历史。
- 支持导入 / 导出本地记录。

### 3.9 免费版与付费版权益

#### 3.9.1 权益对比

| 功能 | 免费版 | 付费版（9.9 元 / 月） |
|---|---|---|
| 使用额度 | 每天 3 篇 | 不限量 |
| 当前页单篇清洗 | 支持 | 支持 |
| 基础 Markdown 导出 | 支持 | 支持 |
| 纯文本导出 | 支持 | 支持 |
| 批量多链接处理 | 不支持 | 支持 |
| 嵌套链接采集 | 不支持 | 支持 |
| 历史记录 | 不保留或仅临时保留 | 支持 |
| 收藏 | 不支持 | 支持 |
| 多设备同步 | 不支持 | 支持 |
| 高级导出 JSON / HTML | 不支持 | 支持 |
| 引用格式 | 仅基础来源 | 完整引用格式 |
| 阅读主题 | 浅色 / 深色 | 浅色 / 深色 / 护眼 |
| 隐私模式 | 基础本地处理 | 更强的隐私保护 |
| 学生模式 | 不支持 | 支持 |
| 语音朗读 | 不支持 | 支持 |
| 去广告和干扰 | 支持 | 支持 |
| 应用内推广位 | 有 | 无 |
| Agent / API | 不支持 | 后续开放 |

#### 3.9.2 默认计费规则

- 免费版每天可成功清洗 3 篇网页。
- 免费版只支持当前页单篇清洗和基础 Markdown / 纯文本导出。
- 免费额度按自然日重置，默认每天 3 篇，可在 AjkAPI 后台调整。
- 付费版为 9.9 元 / 月包月，从 AjkAPI 余额中扣除。
- 默认使用余额计费，不启用积分计划。
- AjkAPI 若支持，也可配置积分计划作为备用方案。

> 采用“每天免费 3 篇”而不是“一次性免费 10 次”，是为了让用户每天都能体验产品，形成使用习惯；同时免费版限制单篇清洗，避免被批量注册和批量爬取滥用。付费版的真正价值点是“不限量、批量、历史收藏、多设备和高级导出”。

#### 3.9.3 计费边界

- 每成功处理 1 个网页，记为 1 次使用额度。
- 当前页提取、批量链接、嵌套链接都按成功处理的页面数计数。
- 提取失败不扣额度。
- 同一 URL 在短时间内重复处理，可命中本地缓存，不重复计费。
- 免费用户每天 3 篇用完后，提示升级，而不是直接停止使用。
- 付费用户触发批量、嵌套、高级导出等能力时，不再重复扣费。

### 3.10 用户账号与设置

- 支持登录 / 注册 AjkAPI 账号。
- 显示当前余额、今日免费剩余篇数、包月到期时间。
- 余额不足时引导充值或续费。
- 支持退出登录。
- 设置项：
  - 默认导出格式。
  - 默认递归深度。
  - 单次最大链接数。
  - 是否开启安全模式。
  - 主题模式。

### 3.11 安全与青少年模式

- 默认不抓取登录后内容。
- 不执行网页脚本。
- 青少年模式支持：
  - 关键词过滤。
  - 域名白名单 / 黑名单。
  - 强制安全阅读。
  - 隐藏不适合学生的内容。
- 对批量采集和嵌套采集设置上限，防止滥用。

---

## 4. 新增吸引力功能

这些功能用于拉开与免费 DeepSeek 的差距。

### 4.1 一键“当前页”提取

不打开聊天框，不写提示词，点击一下就把当前页面变干净。  
这是免费 AI 替代不了的高频体验。

### 4.2 批量链接队列

一次粘贴 10 个、20 个链接，统一处理。  
适合文案工作者做选题、竞品分析、资料整理。

### 4.3 页面内链接发现与采集

打开一个栏目页或列表页，自动找到文章链接，勾选后批量采集。  
比“手动一个个复制到 AI”效率高得多。

### 4.4 原文保真与来源追踪

每篇输出都包含：

- 标题
- 作者
- 发布时间
- 来源域名
- 原始链接
- 提取时间

适合需要引用、校对、追责的写作场景。

### 4.5 引用格式一键复制

复制内容时可选择：

- 只复制正文
- 复制正文 + 来源
- 复制 Markdown 引用块
- 后续支持 GB/T 7714、APA、MLA

### 4.6 阅读统计

显示：

- 字数
- 段落数
- 预计阅读时间
- 来源域名
- 提取时间

### 4.7 去重与缓存

同一链接短时间内重复处理时，直接使用本地结果。  
节省用户时间和免费额度。

### 4.8 本地优先与隐私保护

网页内容默认在本地处理。  
文案工作者、学生处理稿件或作业时，不需要把内容上传给大模型。

### 4.9 好看的专业界面

采用专业阅读产品的设计风格：

- 清晰的工具栏。
- 舒适的阅读排版。
- 浅色 / 深色 / 护眼主题。
- 状态提示明确。
- 批量任务进度直观。

### 4.10 可选 AI 辅助，但不作为核心

后期可增加“AI 摘要、润色、问答”等可选功能。  
但默认关闭，且不得影响原文保真的核心定位。  
若 AjkAPI 提供模型能力，可作为增值功能接入。

---

## 5. 用户流程

### 5.1 首次使用

1. 用户从 GitHub / Gitee 下载扩展包。
2. 按说明加载到 Chrome 或 Edge。
3. 打开扩展，进入登录页。
4. 登录 / 注册 AjkAPI 账号。
5. 看到“今日免费剩余 3 篇”。
6. 打开任意网页，点击“提取当前页”。

### 5.2 当前页提取

1. 用户在目标网页点击扩展图标。
2. 点击“提取当前页”。
3. 扩展读取当前页内容。
4. 本地清洗正文。
5. 展示阅读模式。
6. 用户复制或导出。

### 5.3 批量链接

1. 用户打开扩展。
2. 点击“批量链接”。
3. 粘贴多个 URL。
4. 点击“开始处理”。
5. 显示进度和结果。
6. 用户选择合并导出或分别导出。

### 5.4 嵌套链接采集

1. 用户在列表页打开扩展。
2. 点击“采集本页内链接”。
3. 扩展识别页面链接。
4. 用户勾选目标链接。
5. 设置深度、最大数量、同域名限制。
6. 点击“开始采集”。
7. 查看结果并导出。

### 5.5 计费流程

1. 用户发起一次成功提取。
2. 客户端检查本地状态和 AjkAPI 状态。
3. 若用户已有有效包月，则正常使用，不受每日免费篇数限制。
4. 若用户没有包月，检查是否属于免费版允许的单篇清洗。
5. 若为单篇清洗且今日免费剩余篇数大于 0，则消耗 1 篇今日免费额度。
6. 若今日免费额度用完，或用户使用批量、嵌套、高级导出等付费功能，提示升级。
7. 用户确认后，从 AjkAPI 余额扣除 9.9 元，开通包月。
8. 购买成功后继续处理。

---

## 6. 系统架构

### 6.1 总体架构

```text
┌──────────────────────────── 浏览器扩展 ────────────────────────────┐
│                                                                    │
│  Popup / SidePanel              Background Service Worker           │
│  - 当前页提取                    - 批量任务调度                      │
│  - 批量链接                      - 网页抓取                          │
│  - 嵌套链接选择                  - 正文清洗                          │
│  - 阅读模式                      - 缓存 / 历史                      │
│  - 导出                          - AjkAPI 调用                      │
│                                                                    │
│  Content Script                 Local Storage / IndexedDB           │
│  - 读取当前页                   - 设置、历史、缓存                   │
└────────────────────────────────────────────────────────────────────┘
                        │
                        │ HTTPS
                        ▼
              ┌─────────────────┐
              │   AjkAPI 服务器  │
              │  https://ajkapi. │
              │  9zos.com        │
              │ - 用户 / 登录    │
              │ - 余额 / 月卡    │
│ - 免费额度配置   │
              │ - 消费记录       │
              └─────────────────┘
```

### 6.2 模块划分

| 模块 | 职责 |
|---|---|
| UI 模块 | 弹窗、阅读页、设置页、引导页、批量任务页 |
| 抓取模块 | 请求网页、读取 HTML |
| 清洗模块 | 提取正文、去广告、去干扰、生成 Markdown |
| 导出模块 | 生成 `.md`、`.txt`、`.json`、`.html` |
| 存储模块 | 历史、收藏、设置、缓存 |
| 计费模块 | 调用 AjkAPI，处理免费额度和包月 |
| 安全模块 | 域名过滤、青少年模式、任务限额 |

### 6.3 推荐技术栈

| 部分 | 推荐方案 |
|---|---|
| 扩展框架 | WXT 或 Plasmo |
| 前端 | React + TypeScript |
| 样式 | Tailwind CSS |
| UI 组件 | shadcn/ui 或自研轻量组件 |
| 正文提取 | Mozilla Readability |
| HTML 转 Markdown | Turndown |
| HTML 清理 | sanitize-html |
| 状态管理 | Zustand |
| 本地存储 | IndexedDB / localStorage |
| 打包发布 | GitHub Actions / Gitee Go |

### 6.4 本地处理原则

- 网页正文抓取在用户本地完成。
- 清洗和转换在用户本地完成。
- 服务器只做身份、余额、月卡、免费额度和计费记录。
- 原始网页内容默认不上传到业务服务器。

---

## 7. AjkAPI 集成设计

### 7.1 基础信息

- Base URL：`https://ajkapi.9zos.com`
- 默认计费方式：余额
- 默认价格：9.9 元 / 月
- 默认免费额度：每天 3 篇
- 免费额度配置：支持在 AjkAPI 后台调整
- 积分计划：暂不默认启用，但保留扩展能力

### 7.2 集成原则

1. 不在公开代码中硬编码管理员密钥。
2. 用户登录后获取用户令牌，令牌保存在本地。
3. 所有请求使用 HTTPS。
4. 计费接口必须做失败重试和幂等处理。
5. 客户端无法确认扣费成功时，不得静默扣除用户免费额度。

### 7.3 需要对接的逻辑接口

> 以下为逻辑接口设计，具体路径和字段以 AjkAPI 官方文档为准。

| 逻辑接口 | 用途 |
|---|---|
| 用户登录 / 注册 | 获取用户令牌 |
| 查询余额 | 展示当前余额 |
| 查询权益 | 查询今日免费额度、包月状态、到期时间 |
| 购买月卡 | 从余额扣除 9.9 元，开通 30 天 |
| 上报使用额度 | 成功提取后上报 1 篇额度 |
| 查询消费记录 | 用于账单展示和核对 |

### 7.4 计费状态机

```text
用户发起提取
    │
    ▼
提取是否成功？
    │ 否
    ├─────────────► 不扣额度，提示失败原因
    │
    ▼ 是
是否有有效包月？
    │ 是
    ├─────────────► 不扣额度，正常使用全部付费功能
    │
    ▼ 否
是否属于免费版允许的单篇清洗？
    │ 否
    ├─────────────► 提示“该功能为付费版功能”
    │
    ▼ 是
今日免费剩余篇数是否大于 0？
    │ 是
    ├─────────────► 消耗 1 篇今日免费额度
    │
    ▼ 否
引导购买 9.9 元月卡
    │
    ▼
余额是否足够？
    │ 否
    ├─────────────► 提示余额不足，引导充值
    │
    ▼ 是
扣除余额 9.9 元，开通包月
    │
    ▼
正常使用
```

### 7.5 计费字段建议

| 字段 | 类型 | 说明 |
|---|---|---|
| `freeDailyQuota` | number | 每日免费篇数，默认 3 |
| `freeUsedToday` | number | 今日已使用的免费篇数 |
| `monthlyPrice` | number | 月卡价格，默认 9.9 元 |
| `billingMode` | string | `balance` 或 `points` |
| `subscriptionDays` | number | 包月有效天数，默认 30 |
| `usageCount` | number | 当前周期已使用额度 |
| `balance` | number | 当前余额 |
| `expiresAt` | datetime | 包月到期时间 |

### 7.6 安全与防滥用

- 每日免费额度应在 AjkAPI 服务端按账号重置，客户端只做展示，不能作为唯一依据。
- 新设备登录需要验证。
- 每日免费额度绑定账号，不随重装或清除本地数据无限重置。
- 批量任务设置单次最大链接数。
- 嵌套采集设置深度和总页数上限。
- 同一 URL 短时间缓存，不重复计费。
- 计费接口请求失败时不阻塞本地阅读，但需在恢复后补记。

---

## 8. 数据模型

### 8.1 提取任务

```text
LinkTask
- id
- sourceType: current | manual | nested
- url
- depth
- status: pending | running | success | failed | skipped
- createdAt
- finishedAt
- errorMessage
```

### 8.2 提取结果

```text
ExtractedPage
- id
- taskId
- url
- title
- author
- publishedAt
- lang
- contentMarkdown
- contentText
- contentHtml
- links
- images
- sourceHash
- extractedAt
```

### 8.3 用户状态

```text
UserState
- userId
- ajkToken
- freeUsed
- freeQuota
- balance
- monthlyExpiresAt
- billingMode
- settings
```

### 8.4 本地设置

```text
Settings
- exportFormat
- defaultDepth
- maxLinksPerTask
- safeMode
- theme
- autoExtractCurrentPage
- language
```

---

## 9. UI / UX 规范

### 9.1 界面结构

扩展弹窗尺寸建议：

- 宽度：380px - 420px
- 高度：自适应，最高约 560px

阅读模式建议：

- 在扩展侧边栏或新标签页中打开。
- 阅读区最大宽度约 720px。
- 支持浅色、深色、护眼三种主题。

### 9.2 视觉风格

- 专业、简洁、克制。
- 主色建议使用低饱和蓝或墨绿色。
- 不使用大量动画和装饰。
- 按钮和状态标识清晰。
- 对中小学生友好：图标明确、文字简短、提示温和。

### 9.3 关键状态

| 状态 | 界面表现 |
|---|---|
| 未登录 | 显示登录页，引导注册 |
| 免费额度充足 | 显示“今日免费剩余 X 篇” |
| 免费额度用完 | 显示“今日免费额度已用完，9.9 元/月解锁不限量” |
| 包月有效 | 显示“包月有效期至 X 月 X 日” |
| 余额不足 | 显示“余额不足，请先充值” |
| 提取中 | 显示进度、当前链接、成功/失败数 |
| 提取失败 | 显示失败原因和原链接 |

---

## 10. 发布、安装与使用说明

### 10.1 公开发布内容

GitHub 和 Gitee 均需提供：

1. `README.md`：产品介绍、功能、安装、使用、计费说明。
2. `Release` 下载包。
3. 源码压缩包。
4. `SHA256SUMS.txt` 校验文件。
5. 隐私政策或隐私说明。
6. 常见问题 FAQ。

建议发布包：

```text
cleanread-v1.0.0-chrome.zip
cleanread-v1.0.0-edge.zip
cleanread-v1.0.0-src.zip
SHA256SUMS.txt
README.md
```

### 10.2 安装说明

#### Chrome

1. 下载 `cleanread-v1.0.0-chrome.zip`。
2. 解压到固定文件夹。
3. 打开 `chrome://extensions`。
4. 打开右上角“开发者模式”。
5. 点击“加载已解压的扩展程序”。
6. 选择解压后的文件夹。
7. 点击浏览器右上角扩展图标，将“网页阅读器”固定。

#### Edge

1. 下载 `cleanread-v1.0.0-edge.zip`。
2. 解压到固定文件夹。
3. 打开 `edge://extensions`。
4. 打开左侧“开发人员模式”。
5. 点击“加载解压缩的扩展”。
6. 选择解压后的文件夹。
7. 固定扩展图标。

### 10.3 首次使用说明

1. 打开扩展，点击“登录 / 注册”。
2. 使用 AjkAPI 账号登录。
3. 确认今日免费剩余篇数。
4. 打开任意文章页面。
5. 点击“提取当前页”。
6. 阅读、复制或导出。

### 10.4 计费使用说明

1. 免费版每天可成功清洗 3 篇网页。
2. 今日免费额度用完，或使用批量、嵌套、高级导出等功能时，点击“购买月卡”。
3. 系统从 AjkAPI 余额中扣除 9.9 元。
4. 购买后 30 天内正常使用。
5. 余额不足时，按提示充值。

### 10.5 更新方式

- 手动更新：重新下载最新 Release 包，替换扩展文件夹，在扩展管理页点击“重新加载”。
- 商店更新：后续上架 Chrome / Edge 商店后，可由商店自动更新。

---

## 11. 测试与验收

### 11.1 功能测试

- 当前页提取。
- 多个链接批量提取。
- 嵌套链接采集。
- Markdown / 纯文本 / JSON / HTML 导出。
- 历史、收藏、搜索。
- 每日免费额度扣除与重置。
- 免费版功能限制。
- 包月购买、余额扣除、到期判断。
- 余额不足提示。
- 主题切换、语音朗读、阅读统计。

### 11.2 质量测试

- 至少覆盖 50 个常见中文站点。
- 至少覆盖 20 个常见英文站点。
- 提取成功率目标：90% 以上。
- 单页处理时间目标：普通网页 3 秒内完成。
- 批量 20 个链接不卡死、不崩溃。

### 11.3 安全测试

- 不执行网页脚本。
- 不读取用户登录态和 Cookie。
- 不抓取本地文件。
- 不向非 AjkAPI 域名发送用户内容。
- 青少年模式能拦截明显不适合内容。

---

## 12. 版本路线

### v1.0：可公开下载的 MVP

- 浏览器扩展：Chrome / Edge。
- 当前页提取。
- 多链接批量处理。
- 嵌套链接深度 1。
- 阅读模式。
- Markdown / 纯文本 / JSON / HTML 导出。
- 历史与收藏。
- AjkAPI 登录、余额、每日免费 3 篇、9.9 元月卡。
- GitHub / Gitee 公开发布。

### v1.1：体验增强

- 嵌套深度 2。
- 更智能的正文识别。
- 引用格式。
- 深色 / 护眼模式。
- 语音朗读。
- 标签管理。
- 商店上架。

### v1.2：工作流集成

- 导出 Word / PDF。
- 接入 Notion / Obsidian / 语雀 / 飞书。
- 可选 AI 摘要和问答。
- 更完善的安全过滤。

### v2.0：多端扩展

- Windows / macOS 桌面端。
- 移动端或小程序。
- Agent API。

---

## 13. 风险与待确认事项

### 13.1 必须确认的 AjkAPI 事项

1. 是否支持用户注册和登录，还是只支持管理员 API Key。
2. 是否支持余额查询、余额扣费、月卡购买。
3. 每日免费篇数如何配置和查询。
4. 是否支持用户级令牌，避免公开客户端泄露密钥。
5. 是否有调用频率限制。
6. 是否提供消费记录查询。
7. 余额和免费额度接口的失败重试、幂等规则。

### 13.2 产品风险

- 如果只是偶尔处理一篇文章，用户仍可能使用免费 DeepSeek。
- 应对方式：主打高频、批量、原文保真、导出和来源追踪。
- 公开客户端携带密钥存在安全风险。
- 应对方式：用户令牌 + 后端鉴权，不公开管理员密钥。
- 每日免费 3 篇可能被批量注册或重复清理数据滥用。
- 应对方式：账号绑定设备、限制注册来源、限制批量任务数量。

### 13.3 合规风险

- 不抓取需要登录、付费、验证码的内容。
- 不进行大规模自动爬取。
- 不绕过网站技术限制。
- 明确告知用户不得用于侵权用途。

---

## 14. 成功标准

第一版达到以下标准即可公开发布：

1. 普通用户能在 3 步内完成第一次提取。
2. 当前页、多链接、嵌套链接三个核心流程可用。
3. 常见中文站点提取成功率不低于 90%。
4. 导出格式准确，Markdown 不乱码、不丢标题和正文。
5. AjkAPI 每日免费额度和包月扣费准确。
6. GitHub / Gitee 提供完整下载、安装、使用说明。
7. 界面专业，阅读体验明显优于普通复制粘贴。
